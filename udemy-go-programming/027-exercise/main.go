package main

import "fmt"

func main() {
	a, b, c  := 747, 911, 90210
	
	fmt.Printf("%v as decimal is: %d, as binary is %b, and as hexideicmal is %#X\n", a, a, a, a)
	fmt.Printf("%v as decimal is: %d, as binary is %b, and as hexideicmal is %#X\n", b, b, b, b)
	fmt.Printf("%v as decimal is: %d, as binary is %b, and as hexideicmal is %#X\n", c, c, c, c)
}

/*

write a program that uses print verbs to show the following numbers
● 747
● 911
● 90210
as
● decimal
● binary
● hexadecimal

*/